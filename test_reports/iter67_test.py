# Playwright test for iteration 67 — Insurance Claims dashboard deadline pill.
# Uses backend API directly to mutate task.due_date, then navigates the dashboard
# in the Expo web preview to verify the new ic-deadline-<claimId> pill behavior.
